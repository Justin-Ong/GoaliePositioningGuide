# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: ./Dataset  # dataset root dir
train: ./TrainingImages  # train images (relative to 'path')
val: ./ValidationImages  # val images (relative to 'path')
test:  # test images (optional)

# Classes
names:
  0: stationary_core
  1: slow_core
  2: flipped_core
  3: fast_core